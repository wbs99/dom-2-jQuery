window.jQuery = function(selector) {
    //接受一个选择器，根据这个选择器得到一些元素，返回一个对象，对象有方法可以操作这些元素
    const elements = document.querySelectorAll(selector);
    const api = {
        addClass () {
            console.log(elements);
        }
    };
    return api;
};
